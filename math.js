function soma(a, b) {
    return a + b;
  }

  function somacomposta (a , b , c) {
      return a + b + c;
  }

  function multiplicacao (a, b) {
      return a * b
  }

  function calc (a, b, c) {
      return (a + b) * c;
  }

  module.exports = { soma, somacomposta, multiplicacao, calc}
